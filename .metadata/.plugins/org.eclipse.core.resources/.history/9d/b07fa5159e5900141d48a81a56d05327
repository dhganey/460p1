package creationalPatterns;

public class ReaderFactory implements IReader
{
	public IReader getReader(Country c)
	{
		if (c == Country.GB) //Britain uses XML
		{
			return new XMLReader();
		}
		else
		{
			return 
		}
	}
}
